{
    "annotated_vcf_path": {
      "description": "Path to the annotated VCF file",
      "optional": false
    },
    "ecr_registry": {
      "description": "ECR registry for container images (e.g. <account-id>.dkr.ecr.<region>.amazonaws.com",
      "optional": false
    },
    "aws_region": {
      "description": "AWS region (e.g. us-east-1). must match where source data is located and where workflow is executed",
      "optional": false
    }
  }